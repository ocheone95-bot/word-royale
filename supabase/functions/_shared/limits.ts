// Общие лимиты, используемые несколькими Edge Functions.
// Источник правды — здесь. record-ad-reward проставляет лимит в RPC,
// today-status возвращает то же значение клиенту для UI-счётчика «N left».

// Сколько rewarded ads юзер может посмотреть в сутки. Защита от brute-force
// фарминга бесплатных replay-кредитов через спам-просмотры. Раньше было 3,
// но это душило play-loop: юзер платит за пропущенный показ времени и
// уходит. Подняли до 100 — для живого юзера фактически без лимита, но
// бот-ферму всё равно остановит. UI больше не показывает «N left».
export const ADS_MAX_PER_DAY = 100;
