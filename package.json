{
  "name": "word-royale",
  "version": "0.0.0",
  "private": true,
  "description": "Word Royale — Telegram Mini App. Casual word puzzle with daily challenge and global leaderboard.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "seed:dictionary": "node --env-file=.env.local packages/dictionary/scripts/seed-db.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "supabase": "^2.95.4",
    "vitest": "^4.1.5"
  }
}
